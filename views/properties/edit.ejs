<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Property Details</title>
</head>
<body>
  <form action="/properties/<%=property._id%>?_method=PUT" method="POST">
    <div>
      <label for="title">title</label>
      <input type="text" name="property[title]" id="title" value="<%= property.title %>">
    </div>
    <div>
      <label for="price">price</label>
      <input type="text" name="property[price]" id="price" value="<%= property.price %>">
    </div>
    <div>
      <label for="location">location</label>
      <input type="text" name="property[location]" id="location" value="<%= property.location %>">
    </div>
    <button>Update</button>
  </form>
  <a href="/property/<%= property._id %>">Back to property</a>
</body>
</html>